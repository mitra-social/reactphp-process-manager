# React PHP Process Manager

This package allows to run multiple instances of the same loop in parallel by using the `pcntl` and `posix` PHP 
extensions.

## Requirements

* PHP >=7.1
* `pcntl` extension
* `posix` extension

## Usage

See `examples` directory for some basic use cases.
